# This project is a frontend-only application using local JavaScript, LocalStorage, and a CDN for Vue.js/TailwindCSS.
# No backend dependencies are strictly necessary to run the core UI.
# However, if you plan to extend this with a Python backend or serve it locally using a dedicated tool, the following can be used:

Flask==3.0.0
pytest==7.4.3